Small rounded badge for counts and statuses (e.g. "3 chapters", "Synced").

```jsx
<Badge tone="aqua">3 versions</Badge>
<Badge tone="danger">Offline</Badge>
```

Tones: `aqua`, `mint`, `neutral`, `danger`.
