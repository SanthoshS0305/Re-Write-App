Raised-teal rounded bar that groups the editor's formatting IconButtons.

```jsx
<Toolbar>
  <IconButton><Bold size={18} /></IconButton>
  <IconButton><Italic size={18} /></IconButton>
</Toolbar>
```
