Transparent white toolbar icon button. Dims to 0.45 opacity when `active` (the formatting-active convention). Pass a Lucide icon as children.

```jsx
<IconButton active><Bold size={18} /></IconButton>
```
